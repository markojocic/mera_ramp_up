package com.example.markojocic.mera;

import android.content.Context;
import android.support.v7.widget.RecyclerView;

import android.view.ViewGroup;
import android.view.View;
import android.view.LayoutInflater;
import android.widget.TextView;
import java.util.List;

public class RecyclerViewAdapter {



    /*Context context;
    List<GetDataAdapter> getDataAdapter;


    public  RecyclerViewAdapter(List<GetDataAdapter> getDataAdapter, Context context) {

        super();



        this.getDataAdapter = getDataAdapter;
        this.context = context;

    }
    @Override
    public ViewHolder onCreateViewHolder(ViewGroup parent, int viewType){

        View v = LayoutInflater.from(parent.getContext()).inflate(R.layout.recyclerview_items, parent, false);

        ViewHolder viewHolder = new ViewHolder(v);

        return viewHolder;
    }

    @Override
    public int getItemCount() {

        return getDataAdapter.size();
    }

    class ViewHolder extends RecyclerView.ViewHolder{

        public TextView repositoryNameTextView;
        public TextView loginNameTextView;
        public TextView repositorySizeTextView;



        public ViewHolder(View itemView) {

            super(itemView);

            repositoryNameTextView = (TextView) itemView.findViewById(R.id.textView2) ;
            loginNameTextView = (TextView) itemView.findViewById(R.id.textView4) ;
            repositorySizeTextView = (TextView) itemView.findViewById(R.id.textView6) ;



        }*/
    }
